<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>LinkedIn_Profile_Requires_Email</fullName>
    <active>true</active>
    <description>Contributors with a LinkedIn profile must have an email address.</description>
    <errorConditionFormula>AND(
NOT(ISBLANK(LinkedIn_Profile__c)),
ISBLANK(Email__c)
)</errorConditionFormula>
    <errorDisplayField>Email__c</errorDisplayField>
    <errorMessage>Email is required when a LinkedIn Profile is provided.</errorMessage>
</ValidationRule>
